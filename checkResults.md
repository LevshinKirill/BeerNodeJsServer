In description should have written that `npm install` is required
Should have mentioned URL to launch app at
camelCase should be used in fields naming
typo in class name
reduce is written hard to read
